<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" charset="utf-8">
    <title><%= content_for?(:title) ? yield(:title) : "subscribe to e.m. rush" %></title>
      <script type="text/javascript"
      src="https://maps.googleapis.com/maps/api/js?key=AIzaSyCA1GYmGXezjpFAXkESQ0uB-DsL34WUduM&sensor=false">
    </script>
    <script type="text/javascript">
      function initialize() {
        var mapOptions = {
          center: new google.maps.LatLng(-34.397, 150.644),
          zoom: 8
        };
        var map = new google.maps.Map(document.getElementById("map-canvas"), mapOptions);
      }
      google.maps.event.addDomListener(window, 'load', initialize);
    </script>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "a place to subscribe to my newsletter" %>">
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>"
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>

    <div class="container">
      <div class="row">
        <div class="col-lg-10 text-center">
           <main role="main">
              <%= render 'layouts/messages' %>
              <%= yield %>
        </main>
        </div>
      </div>
    </div>


  </body>
</html>
